var name = "Joe"
var height = "74"

if (name === "Susan" & height === "74") {
    console.log("The name is correct")
} else {
    console.log("Expected the name to be Susan amd height: "+ parseInt(height))
}

// Don't worry about this
module.exports = { name, height
}
